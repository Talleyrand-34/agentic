{
  "name": "{{WORKFLOW_NAME}}",
  "nodes": [
    {
      "name": "Webhook Trigger",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [240, 300],
      "parameters": {
        "path": "{{WEBHOOK_PATH}}",
        "httpMethod": "POST",
        "responseMode": "lastNode",
        "options": {}
      }
    }
  ],
  "connections": {},
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "meta": {
    "templateDescription": "Basic webhook trigger stub. Add downstream nodes and connect them.",
    "variables": {
      "WORKFLOW_NAME": "Human-readable workflow name",
      "WEBHOOK_PATH": "URL path segment for the webhook (e.g. 'my-workflow')"
    }
  }
}
